# Building a list manually
# Building a list using input()
# Building a list using loops and random
# Built-in functions that use lists: min(), max(), sum(), len(), sorted()
# Some more builtin functions: abs(), type(), 
# Custom Mean, media, mode, and range functions
# Now, write them without min(), max(), or sum()
# Create a loading bar using loops